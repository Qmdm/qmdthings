```{R}
#| include: false
knitr::knit_engines$set(venn = function(options) {
  options$code <- c(
    "\\tikzset{",
    "  every node/.style={font=\\sffamily\\normalsize},",
    "  every path/.style={line width=1.5pt}",
    "}",
    options$code
  )
  options$engine.opts <- list(
    extra.preamble = c(
      "\\usepackage{tikz}",
      "\\usepackage{venndiagram}"
    )
  )
  knitr::knit_engines$get("tikz")(options)
})
```